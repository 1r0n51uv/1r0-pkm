<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- TEMPORARY: the backend is plain HTTP on a bare IP (no domain/TLS yet).
	     Scoped exception for that one host only — remove once the backend has
	     a hostname + HTTPS (Caddy auto-cert). Other Info.plist keys are still
	     generated by GENERATE_INFOPLIST_FILE=YES and merged on top of this. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSExceptionDomains</key>
		<dict>
			<key>100.31.154.129</key>
			<dict>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<true/>
			</dict>
		</dict>
	</dict>
</dict>
</plist>
